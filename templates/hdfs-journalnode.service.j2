[Unit]
Description=HDFS JournalNode service 

[Service]
Type=simple
User={{ hadoop_user }}
Group={{ hadoop_user }}
ExecStart={{ hadoop_unarchive_prefix }}/hadoop-{{ hadoop_version }}/bin/hdfs journalnode
StandardOutput=journal
StandardError=journal
Restart=always

[Install]
WantedBy=multi-user.target
